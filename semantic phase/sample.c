#include <stdio.h>

/* No errors just testing if-else, if-else ladder */
int sum(int ab1,int ab2){
  return ab1+ab2;
}
int d = 3;
int main(float abc){
   char a = 1;
   int b = 2;
   int c = 3;
   char arr[3];
   int max;
   if(a >= b){
       int arr[7];
       int ask = 0;
       if(a >= c){
           max = a;
       }
       else{
	     int aws;
           max = c;
           ask = 0;
       }
       ask = 3;
   }
   else{
       //ask = 2;
       if(b >= c){
           max = b;
       }
       else{
           max = c;
       }
   }

   if(max > 2){
       int d = 0;
       printf("Good! \n");
   }
   d = 3;
   return 0;
}
