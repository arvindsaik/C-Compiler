#include <stdio.h>

/* No errors just testing if-else, if-else ladder */
int sum(float ab1,int ab2){
  return ab1+ab2;
}
unsigned int d = 3;
int main(float abc){
   int a = 1;
   float b = 2;
   int c = 3;
   //int as = sum(10.0,20.0);
   char arr[4];
   //arr[2] = 'c';
   arr = 'c';
   int max;
   if(a >= b){
       int arr[7];
       int ask = 0;
       if(a >= c){
           max = a;
       }
       else{
	     int aws;
           max = c;
         }
   }
   else{
       //ask = 2;
       if(b >= c){
           //max = b;
       }
       else{
           max = c;
       }
   }
   if(max > 2){
       int d = 0;
       printf("Good! \n");
   }
   char pq;
   a = sum(a, 3);

   int sm = 10 + 'c';
  //  sum(1.5, 2-4);

   d = 3;
   return 0;
}
